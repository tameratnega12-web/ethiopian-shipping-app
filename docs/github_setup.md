# GitHub Setup

1. Create a new repository named `ethiopian-shipping-app`.
2. Upload all files in this folder, not the ZIP file.
3. Go to Settings → Pages.
4. Choose GitHub Actions as the deployment source.
5. Push to the `main` branch. The included workflow deploys the app.

Do not upload private `.env` files or secret keys.
